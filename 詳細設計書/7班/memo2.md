- 予約申請

```mermaid
%%{init: {'flowchart': {'curve': 'linear'}}}%%
flowchart TD
    A([申請ボタン押下]) --> B[検索キーワード確認]
    B --> C{検索条件が1項目以上入力されている？}

    C --> |はい|D[入力されている項目は99文字以内か？]
    C -->|いいえ| F[エラーメッセージ「100⽂字を超えた入力はできません」を表示]

    D -->|はい|E[データベースを検索し、検索結果を取得]
    D -->|いいえ| G[エラーメッセージ「検索内容を⼊⼒してください」を表示]

    E --> H([終了])
    F --> H
    G --> H
```